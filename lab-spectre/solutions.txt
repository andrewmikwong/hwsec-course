Discussion Question 1: How many addresses need to be flushed at the first step?
We would need to flush 256 addresses, one for each possibly pyte value. Since the victim accesses the shared memory, each possible value of secret_data from 0x00 to 0xFF maps to a different page-sized offset in the shared memory region. To make the later reload significant, we should evict all candidate locations evicted first so that the one the victim touches stands out as a cache hit 

Discussion Question 2: Now assume the attacker and victim no longer share a memory region.
Would your attack still work? If not, what changes would you need to make to make it work?


Not with Flush + Reload in the same form as it depends on a shared memory space that the attacker can access. Instead of a shared memory region, the attacker would need to switch to a cache side channel attack, focusing on same-cache-set monitoring instead of shared memory


Discussion Question 3: In our example, the attacker tries to leak the values in the array secret_part2. In a real-world attack, people can use Spectre to leak arbitrary values in the victim’s address space. Explain how the attacker can achieve this
In this lab, the out-of-bounds access is aimed at a specific secret array, but the same spectre patter can be generalized to leak other values beause the transient load does not have to come from a fixed "secret array". The attacker's real goal is to make speculative execution read a byte from some target address and use the byte to influence a later memory access where the cache effects can be observed. The second step is the access into shared memory at an offset derived from the loaded byte, which turns the secret value into a measurable cache signal. This can be related to a real attack if the attacker can control or influence the pointer or index during speculation and can make the victim transietnly read from the other locations in the victim's address space and encode those bytes through the cache the same way. 

Discussion Question 4: Try to tune the training parameters and answer this question: What is the fewest number of times you need to train the branch on line 9 in Listing 3 to make the attack work?
After trying a training process of 1 run, we find that this is the minimum number of times we need to train the branch

Discussion Question 5: Describe the strategy you employed to extend the speculation window of the target branch in the victim.
In order to implement the attack described in Part 3, I increased the effective prediction time length by heavily training the branch predictor through repeated access to in-bound data before performing the out-of-bounds access. Since we no longer flush our bound variable in the attack as we did in Part 2, I created an eviction buffer based on a strided access pattern to flush out as much cache information from private caches as possible, therefore making the victim's ability to resolve control flow slower and less predictable. After performing the training, I flushed the shared memory side-channel pages, made a single out-of-bounds kernel call and then reloaded all of my candidate pages to determine which of them had been brought back into cache through speculation. By running this process over many trials, I was able to build up scores for each possible byte value and ultimately retrieve the secret. My approach was therefore a combination of branch predictor training, cache eviction, and repeated scoring, creating a strong enough speculative signal.

Discussion Question 6: Assume you are an attacker looking to exploit a new machine that has the same kernel module installed as the one we attacked in this part. What information would you need to know about this new machine to port your attack? Could it be possible to determine this infomration experimentally? Briefly describe in 5 sentences or less.
To port the attack to a new machine, I would need to know the cache hierarchy. Specifically, the sizes, line size, and likely eviction behavior, cache hit/miss timing thresholds, and how the CPU’s branch predictor and speculative execution behave. I would also need to retune attack parameters such as the cache-hit threshold, number of training calls, number of trials, and eviction-buffer size/stride. This can be mostly determined experimentally by running timing tests and microbenchmarks on the new system. By tuning these parameters, I would be able to adapt the attack even without detailed hardware documentation.